apiVersion: apps/v1
kind: Deployment
metadata:
  name: pwn-flow3
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pwn-flow3
  template:
    metadata:
      labels:
        app: pwn-flow3
    spec:
      enableServiceLinks: false
      automountServiceAccountToken: false
      containers:
        - name: pwn-flow3
          image: keanekwa/repctf:pwn-flow3
          ports:
            - containerPort: 5000
          securityContext:
            privileged: true
---
apiVersion: v1
kind: Service
metadata:
  name: pwn-flow3
spec:
  type: ClusterIP
  selector:
    app: pwn-flow3
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
