apiVersion: apps/v1
kind: Deployment
metadata:
  name: misc-jail3
spec:
  replicas: 2
  selector:
    matchLabels:
      app: misc-jail3
  template:
    metadata:
      labels:
        app: misc-jail3
    spec:
      containers:
        - name: oracle
          image: keanekwa/repctf:misc-jail1
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: misc-jail3
spec:
  type: ClusterIP
  selector:
    app: misc-jail3
  ports:
    - protocol: TCP
      name: misc-jail3
      port: 1342
      targetPort: 5000
      protocol: TCP
